---
name: CI

on:
  push:
    branches:
      - '**'

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: voidzero-dev/setup-vp@v1
        with:
          node-version: '22'
          cache: true
      - name: Install dependencies
        run: vp install --frozen-lockfile
      - name: YAML Check (Prettier)
        run: vp exec prettier --check "**/*.{yml,yaml}"
      - name: Vite+ Check (lint & format)
        run: |
          vp check --fix
          git diff --exit-code

  build-check:
    runs-on: ${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        platform: [macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v6
      - uses: voidzero-dev/setup-vp@v1
        with:
          node-version: '22'
          cache: true
      - uses: dtolnay/rust-toolchain@stable
      - uses: swatinem/rust-cache@v2
        with:
          workspaces: ./src-tauri -> target
      - name: Install just
        uses: taiki-e/install-action@just
      - run: vp install --frozen-lockfile
      - name: Build (debug mode)
        run: vp exec tauri build --debug --no-bundle
