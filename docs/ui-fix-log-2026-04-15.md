# UI fix log (2026-04-15)

Tracks implementation progress for the 2026-04-15 regression batch.

## Planned work items

1. Fix Java uninstall to remove actual installed runtime directory
2. Fix Files save regression (button + shortcut path)
3. Align plugin platform animation with app page transition style
4. Replace Paper platform logo with local asset
5. Improve Files toolbar icon visibility (SVG + UI)
6. Fix Proxy backend detail spacing
7. Improve Java modal wrapping/layout
8. Improve normal toast readability
9. Improve light theme consistency across all views
10. Upgrade backup selector graph to zoom/pan/rearrange interaction

## Progress

- [x] Created dedicated branch for this batch (`fix/ui-polish-2026-04-15`)
- [x] Added research and fix-log documents
- [x] Implemented code fixes
- [x] Validation completed (`pnpm check && pnpm build`)
- [ ] PR/review follow-up in progress

## Implemented changes

1. **Java uninstall residue**
   - Updated deletion logic to remove managed install roots safely (`src/lib/java-commands.ts`)
   - Added explicit uninstall failure toast handling (`JavaManagerModal`, i18n keys)

2. **Files save regression**
   - Switched editor state to keep full file path + name pair
   - Save now uses stable opened path instead of recomposed string during save action

3. **Proxy spacing issue**
   - Backend name/detail now render with explicit block layout and readable spacing

4. **Java modal wrapping**
   - Improved responsive grid, button truncation behavior, and narrow-screen layout

5. **Plugin animation + Paper logo**
   - Platform switch animation aligned to app page transition style
   - Paper ecosystem logo switched to local `src/assets/papermc_logo.svg`
   - Search placeholder fallback hardened to avoid `{}` artifacts

6. **Files icon visibility**
   - Backed up original SVGs to `src/assets/icons/_backup-2026-04-15/`
   - Recolored icon assets and adjusted toolbar button surfaces for better contrast

7. **Toast readability**
   - Normal toast backgrounds changed to high-opacity gradients with borders/backdrop blur

8. **Select visual alignment**
   - Updated shared select control line-height/padding/option metrics
   - Added light-theme option color overrides

9. **Backup selector graph interactions**
   - Added graph zoom controls, wheel zoom, panning, and node/group drag repositioning
   - Preserved tree mode and existing apply payload semantics

10. **Light-theme global polish**

- Added cross-view light theme override layer for major panels and text contrast
- Registered override sheet in `src/styles/index.scss`
